# Browser requests stay same-origin and are forwarded by Next.js rewrites.
NEXT_PUBLIC_API_BASE_URL=/api

# Used by Server Components and the development rewrite target.
SERVER_API_BASE_URL=http://localhost:8080/api
