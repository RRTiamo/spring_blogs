# Keep the browser on the public site origin. OpenResty forwards /api to Java.
NEXT_PUBLIC_API_BASE_URL=/api

# Internal address used only by the Next.js server on the production host.
SERVER_API_BASE_URL=http://127.0.0.1:8080/api
